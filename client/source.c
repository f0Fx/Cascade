#include <stdio.h>

int main(){
    // Allow the client to connect to the server
    // Check if client is subscribed to the service
    // If yes, download encrypted .dll into memory
    // Upon injection, the .dll will decrypt line by line and execute to avoid detection
    
    return  0;
}